import logo from '../../assets/logo.svg'
const Header = () => {
    return (
        <nav>
            <div>
                <img src={logo} alt="" />
            </div>
        </nav>
    );
};

export default Header;